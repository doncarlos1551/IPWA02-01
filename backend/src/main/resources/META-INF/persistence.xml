<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="HeroToZeroPersistenz" transaction-type="JTA">
        <jta-data-source>java:/HeroToZero</jta-data-source>
        
        <class>model.Benutzer</class>
        <class>model.Land</class>
        <class>model.EmissionsDaten</class>
        
        <properties>
            <!-- Aktualisiere das Schema basierend auf den Entity-Klassen -->
            <property name="jakarta.persistence.schema-generation.database.action" value="update"/>

            <!-- Optional: Zeige SQL-Statements in der Konsole an -->
            <property name="hibernate.show_sql" value="true"/>

            <!-- Weitere spezifische Hibernate-Eigenschaften, falls erforderlich -->
            <!-- <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/> -->
        </properties>
    </persistence-unit>
</persistence>
